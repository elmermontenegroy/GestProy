<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gestproy.mybatis.dao.MUsuarioMapper">

	<insert id="registrarCliente" parameterType="cliente" useGeneratedKeys="true" keyProperty="codigo">
		insert into tb_cliente (codigo, dni, nombre, apellido, edad, correo, celular)
		values (#{codigo}, #{dni}, #{nombre}, #{apellido}, #{edad}, #{correo}, #{celular})
		<selectKey order="AFTER" resultType="int" keyProperty="codigo">
			select LAST_INSERT_ID();
		</selectKey>
	</insert>

	<update id="actualizarCliente" parameterType="cliente">
		update tb_cliente set dni = #{dni},
		 				      nombre = #{nombre}, 
		 				      apellido = #{apellido}, 
		 				      edad = #{edad}, 
		 				      correo = #{correo}, 
		 				      celular = #{celular}
		where codigo = #{codigo}
	</update>

	<delete id="eliminarCliente" parameterType="int">
		delete from tb_cliente where codigo = #{codigo}
	</delete>

	<select id="listarClientes" resultType="cliente">
		select codigo, dni, nombre, apellido, edad, correo, celular 
		from tb_cliente 
	</select>

	<select id="obtenerUsuarioPorCodigo" 
			resultType="UsuarioDTO"
			parameterType="int">
		select UsuarioId, usuario, clave, email, nombre, apellido, fechaNacimiento,tipoUsuarioId, estado from tb_usuario
		where UsuarioId = #{UsuarioId}
	</select>

</mapper>